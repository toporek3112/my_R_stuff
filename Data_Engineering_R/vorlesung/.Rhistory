library(esquissse)
install.packages("esquisse")
library(esquissse)
library(esquisse)
library(esquissse)
library(esquissse)
library(esquisse)
library(esquissse)
esquisse::esquisserServer(data=iris)
esquisse::esquisse_server(data=iris)
pairs(iris)
pairs(iris, col=iris$Species)
library(esquissse)
esquisse::esquisse_server(data=iris)
esquisse::esquisser(data=iris)
ggraptR::ggraptR(data=iris)
ggraptR::ggraptR(iris)
esquisse::esquisser(data=iris)
ggraptR::ggraptR(iris)
ggpairs(
iris,
mapping = aes(colour = Species, alpha = 0.5),
columns = c(
"Sepal.Length",
"Sepal.Width",
"Petal.Length",
"Petal.Width",
"Species"
),
upper = list(
continuous = "cor",
combo = "box",
discrete = "facetbar"
),
diag = list(continuous = "densityDiag", discrete = "barDiag"),
lower = list(
continuous = "points",
combo = "facethist",
discrete = "facetbar"
)
)ggpairs(
ggpairs(
iris,
mapping = aes(colour = Species, alpha = 0.5),
columns = c(
"Sepal.Length",
"Sepal.Width",
"Petal.Length",
"Petal.Width",
"Species"
),
upper = list(
continuous = "cor",
combo = "box",
discrete = "facetbar"
),
diag = list(continuous = "densityDiag", discrete = "barDiag"),
lower = list(
continuous = "points",
combo = "facethist",
discrete = "facetbar"
)
)ggpairs(
ggpairs(
iris,
mapping = aes(colour = Species, alpha = 0.5),
columns = c(
"Sepal.Length",
"Sepal.Width",
"Petal.Length",
"Petal.Width",
"Species"
),
upper = list(
continuous = "cor",
combo = "box",
discrete = "facetbar"
),
diag = list(continuous = "densityDiag", discrete = "barDiag"),
lower = list(
continuous = "points",
combo = "facethist",
discrete = "facetbar"
)
)
ggpairs(
iris,
mapping = aes(alpha = 0.5),
columns = c(
"Sepal.Length",
"Sepal.Width",
"Petal.Length",
"Petal.Width",
"Species"
),
upper = list(
continuous = "cor",
combo = "box",
discrete = "facetbar"
),
diag = list(continuous = "densityDiag", discrete = "barDiag"),
lower = list(
continuous = "points",
combo = "facethist",
discrete = "facetbar"
)
)ggpairs(
ggpairs(
iris,
mapping = aes(colour = Species, alpha = 0.5),
columns = c(
"Sepal.Length",
"Sepal.Width",
"Petal.Length",
"Petal.Width",
"Species"
),
upper = list(
continuous = "cor",
combo = "box",
discrete = "facetbar"
),
diag = list(continuous = "densityDiag", discrete = "barDiag"),
lower = list(
continuous = "points",
combo = "facethist",
discrete = "facetbar"
)
)
ggpairs(
iris,
mapping = aes(alpha = 0.5),
columns = c(
"Sepal.Length",
"Sepal.Width",
"Petal.Length",
"Petal.Width",
"Species"
),
upper = list(
continuous = "cor",
combo = "box",
discrete = "facetbar"
),
diag = list(continuous = "densityDiag", discrete = "barDiag"),
lower = list(
continuous = "points",
combo = "facethist",
discrete = "facetbar"
)
)
ggpairs(
iris,
mapping = aes(alpha = 0.5),
columns = c("Sepal.Length", "Sepal.Width", "Petal.Length", "Petal.Width"),
upper = list(
continuous = "cor",
combo = "box",
discrete = "facetbar"
),
diag = list(continuous = "densityDiag", discrete = "barDiag"),
lower = list(
continuous = "points",
combo = "facethist",
discrete = "facetbar"
)
)
ggpairs(
iris,
mapping = aes(alpha = 0.5),
columns = c("Sepal.Length", "Sepal.Width", "Petal.Length"),
upper = list(
continuous = "cor",
combo = "box",
discrete = "facetbar"
),
diag = list(continuous = "densityDiag", discrete = "barDiag"),
lower = list(
continuous = "points",
combo = "facethist",
discrete = "facetbar"
)
)
ggplot(iris, aes(y = Sepal.Width, x = Sepal.Length)) + geom_point(
stat = "identity",
position = "jitter",
alpha = 0.5,
size = 3
) + theme_grey() + theme(text = element_text(
family = "sans",
face = "plain",
color = "#000000",
size = 15,
hjust = 0.5,
vjust = 0.5
)) + scale_size(range = c(1, 3)) + xlab("Sepal.Length") + ylab("Sepal.Width")
mtcars
data <- mtcars %>%  mutate(efficiency = mpg/wt, .before = cyl) %>%
mutate(am = as.factor(am), .before = cyl)
ggraptR(data)
ggraptR:ggraptR(data)
data <- mtcars %>%  mutate(efficiency = mpg/wt, .before = cyl) %>%
mutate(am = as.factor(am), .before = cyl)
ggraptR::ggraptR(data)
ggraptR::ggraptR(data)
data <- mtcars %>%  mutate(efficiency = mpg/wt, .before = cyl) %>%
mutate(am = as.factor(am., levels=c("auto", "manual")), .before = cyl)
data <- mtcars %>%  mutate(efficiency = mpg/wt, .before = cyl) %>%
data$am = ifelse(data$am == 0, "Automatik", "Manuell")
data <- mtcars %>%  mutate(efficiency = mpg/wt, .before = cyl) %>%
data$am = ifelse(data$am == 0, "Automatik", "Manuell")
data <- mtcars %>%  mutate(efficiency = mpg/wt, .before = cyl)
data$am <- ifelse(data$am == 0, "Automatik", "Manuell")
ggraptR::ggraptR(data)
